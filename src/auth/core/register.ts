import { user } from '../db/schema';
import { AuthContext } from './type';

export function registerUserWithPassword(context: AuthContext) {

  const { db } = context;

  // db.insert(user).values({
  //   id:
  // });

}
